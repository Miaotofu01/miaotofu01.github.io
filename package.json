{
  "name": "blog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsx scripts/build.ts && vite build --config vite.build.ts",
    "build:ssg": "tsx scripts/build.ts",
    "build:bundle": "vite build --config vite.build.ts",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0",
    "tsx": "^4.0.0"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "lit": "^3.2.0",
    "marked": "^15.0.0",
    "shiki": "^2.0.0"
  }
}
